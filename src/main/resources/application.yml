spring:
  application:
    name: db-to-csv-practice

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: jhkang
    password: 0000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: never
